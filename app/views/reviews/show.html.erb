<p style="color: green"><%= notice %></p>
<h3 class="text-center">Récapitulatif de l'avis</h3>
<%= render @review %>

<div class="mt-6 d-flex justify-content-center">
  <% if current_user.id == @review.rental.renter.id %>
    <%= link_to "Modifier l'avis", edit_review_path(@review), class: "btn btn-primary mx-3" %>
    <%= button_to "Supprimer cet avis", @review, method: :delete, class: "btn btn-danger" %>
  <% end %>  
</div>

<div class="d-flex justify-content-center">
  <%= link_to "Accueil", root_path, class: "btn btn-primary" %>
</div>
