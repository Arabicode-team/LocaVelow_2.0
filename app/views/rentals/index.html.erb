
<h1>Rentals</h1>

<div id="rentals">
  <% @rentals.each do |rental| %>
    <%= render rental %>
    <p>
      <%= link_to "Show this rental", rental, aria_label: "Show rental" %>
    </p>
  <% end %>
</div>

<%= link_to "New rental", new_rental_path, aria_label: "New rental" %>
