<nav class="navbar navbar-expand-lg blur blur-rounded top-0 border-bottom z-index-3 shadow w-100 mt-4 d-none d-lg-block my-3 py-2">
    <div class="container-fluid d-flex justify-content-center align-items-center">
        <ul class="navbar-nav d-flex align-items-center">
            <li class="nav-item">
                <%= link_to "Je suis propriétaire", new_user_registration_path, class: "btn btn-outline-light", style: "background-color: #FFDE8C; color: #506191; border-radius: 25px;" %>
            </li>
            <li class="nav-item ms-4">
                <%= link_to "Je cherche un vélo", new_user_registration_path, class: "btn btn-outline-light", style: "background-color: #506191; color: #FFDE8C; border-radius: 25px;" %>
            </li>
        </ul>
        <a href="/" class="text-center">
            <img src="<%= asset_path('logo.png') %>" class="logo-img" width="10%" alt="Logo">
        </a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <% if user_signed_in? %>
                        <%= link_to "Profile", user_path(current_user), class: "nav-link" %>
                    <% else %>
                        <%= link_to "Register", new_user_registration_path, class: "nav-link" %>
                    <% end %>
                </li>
                <li class="nav-item">
                    <% if user_signed_in? %>
                        <%= form_tag(destroy_user_session_path, method: :delete) do %>
                            <%= submit_tag "Logout", class: "btn btn-outline-light", style: "background-color: #FFDE8C; color: #506191; border-radius: 25px;" %>
                        <% end %>
                    <% else %>
                        <%= link_to "Se connecter", new_user_session_path, class: "btn btn-custom", style: "background-color: #506191; color: #FFDE8C; border-radius: 25px;" %>
                    <% end %>
                </li>  
            </ul>
        </div>
    </div>
</nav>