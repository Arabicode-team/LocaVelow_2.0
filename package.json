{
  "name": "app",
  "private": "true",
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo-rails": "^8.0.0-beta.1",
    "esbuild": "^0.19.8"
  },
  "scripts": {
    "build": "esbuild app/javascript/packs/*.js --bundle --outdir=app/assets/builds && esbuild app/javascript/application.js --bundle --outdir=app/assets/builds"
  }
}
